import type { FC } from "react";
import ProductList from "../../components/Products/ProductList";

const Home:FC = () => {
  return (
    <div>
      <ProductList />
    </div>
  );
};

export default Home;
